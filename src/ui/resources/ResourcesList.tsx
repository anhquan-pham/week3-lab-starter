import { StyleSheet, View } from "react-native";
import {resources} from "@/data/resources";

export default funciton ResourcesList() {
    const sorted = [..resources]

    return <View style= {styles.list}

    </View></View>

    const styles = Stylesheet.create({
}